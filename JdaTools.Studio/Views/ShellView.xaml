<Window x:Class="JdaTools.Studio.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JdaTools.Studio" xmlns:views="clr-namespace:JdaTools.Studio.Views" xmlns:viewmodels="clr-namespace:JdaTools.Studio.ViewModels"
        mc:Ignorable="d"
        Title="JDA EXPLORER" Height="450" Width="800">
    <Window.DataContext>
        <viewmodels:ShellViewModel/>
    </Window.DataContext>
    <Window.InputBindings>
        <KeyBinding Modifiers="Control"
                    Key="Return"
                    Command="{Binding ExecuteCurrentTabCommand}" />
    </Window.InputBindings>
    <Grid>
        <Grid IsEnabled="{Binding IsEnabled}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="NEW EDITOR" Command="{Binding NewEditorCommand}"/>
            </StackPanel>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <views:TableExplorerView DataContext="{Binding TableExplorerViewModel}" Grid.Column="0"/>
                <GridSplitter Grid.Column="1" Margin="2,0,2,0"
                  Width="3" 
                  VerticalAlignment="Stretch" 
                  HorizontalAlignment="Center"/>
                <TabControl Grid.Column="2" ItemsSource="{Binding QueryViewModels}" SelectedItem="{Binding SelectedEditor}">
                    <TabControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="QUERY">

                            </TextBlock>
                        </DataTemplate>
                    </TabControl.ItemTemplate>
                    <TabControl.ContentTemplate>
                        <DataTemplate>
                            <views:QueryView DataContext="{Binding}"></views:QueryView>
                        </DataTemplate>
                    </TabControl.ContentTemplate>
                </TabControl>
            </Grid>
        </Grid>
        <Grid Background="AliceBlue" Visibility="{Binding LoginVisibility}">
            <views:LoginView DataContext="{Binding LoginViewModel}"
                VerticalAlignment="Center" HorizontalAlignment="Center" Padding="5"/>
        </Grid>
    </Grid>
</Window>
